"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.BaseApiClient = void 0;
const api_1 = require("./api");
class BaseApiClient {
    constructor(params) {
        if (!params.apiKey)
            throw new Error(`API key is required`);
        if (!params.apiSecret)
            throw new Error(`API secret is required`);
        this.apiKey = params.apiKey;
        this.apiSecret = params.apiSecret;
    }
    async init() {
        const userPass = { Username: this.apiKey, Password: this.apiSecret };
        const poolData = { UserPoolId: this.getPoolId(), ClientId: this.getPoolClientId() };
        this.api = await api_1.createAuthenticatedApi(userPass, poolData, this.getApiUrl());
    }
    async apiCall(fn) {
        if (!this.api)
            await this.init();
        try {
            return await fn(this.api);
        }
        catch (error) {
            // this means ID token has expired so we'll recreate session and try again
            if (error.response.status === 401 && error.response.statusText === 'Unauthorized') {
                await this.init();
                return await fn(this.api);
            }
            throw error;
        }
    }
}
exports.BaseApiClient = BaseApiClient;
